[build-system]
requires = ["maturin>=1.9,<2"]
build-backend = "maturin"

[project]
name = "beamz"
version = "0.5.1"
dynamic = []
authors = [
    { name = "Quentin Wach", email = "quentin.wach+beamz@gmail.com" },
]
description = "Open GPU-accelerated FDTD for photonic chip designers."
readme = "README.md"
requires-python = ">=3.10,<3.15"
license = { file = "LICENSE" }
keywords = ["computational physics", "electromagnetics", "photonics", "inverse design", "simulation"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Physics",
]
dependencies = [
    "numpy>=1.24.4",
    "scipy>=1.10.1",
    "shapely>=2.0.6",
    "jax>=0.4.0",
    "optax>=0.1.0",
    "matplotlib>=3.8.0",
    "scikit-image>=0.21.0",
    "xarray>=2024.0.0",
]

[project.urls]
Homepage = "https://github.com/beamzorg/beamz"
Repository = "https://github.com/beamzorg/beamz"
Documentation = "https://www.beamz.tech/docs/index"
"Bug Tracker" = "https://github.com/beamzorg/beamz/issues"

[project.optional-dependencies]
gds = [
    "gdsfactory>=8.0; python_version < '3.14'",
    "gdsfactory>=9.0; python_version >= '3.14'",
]
gdsfactory = [
    "gdsfactory>=8.0; python_version < '3.14'",
    "gdsfactory>=9.0; python_version >= '3.14'",
]
mesh = [
    "meshio>=5.3",
    "trimesh>=4.5",
]
dev = [
    "ipykernel>=6.29.0",
    "maturin>=1.9,<2",
    "meshio>=5.3",
    "nbconvert>=7.16.0",
    "trimesh>=4.5",
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "myst-parser>=2.0.0",
    "mkdocs>=1.5.0",
    "mkdocs-material>=9.0.0",
    "pyright>=1.1.400",
    "ruff>=0.14.0",
    "vulture>=2.14",
]
lint = [
    "pyright>=1.1.400",
    "ruff>=0.14.0",
    "vulture>=2.14",
]
ubc = [
    "gdsfactory>=8.0; python_version < '3.14'",
    "gdsfactory>=9.0; python_version >= '3.14'",
    "ubcpdk==2.7.0; python_version < '3.14'",
]
test = [ # Adding a 'test' extra that includes pytest and pytest-cov for convenience
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "pytest-randomly>=3.0.0",
    "hypothesis>=6.100.0",
    "diff-cover>=9.0.0",
]


[tool.ruff]
line-length = 88
target-version = "py310"
extend-exclude = ["*.ipynb"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "B", "C90", "SIM"]

[tool.ruff.lint.per-file-ignores]
"beamz/**/__init__.py" = ["F401"]
"vulture_allowlist.py" = ["B018", "F821"]

[tool.pyright]
include = ["beamz"]
typeCheckingMode = "basic"
pythonVersion = "3.10"
reportMissingTypeStubs = false
strict = [
    "beamz/devices/ports.py",
]

[tool.ruff.lint.mccabe]
max-complexity = 35

[tool.maturin]
manifest-path = "rust/fdtd-raster-py/Cargo.toml"
python-source = "."
module-name = "beamz.design.raster._native"
features = ["pyo3/extension-module"]
include = [
    { path = "cuda/CMakeLists.txt", format = "sdist" },
    { path = "cuda/README.md", format = "sdist" },
    { path = "cuda/abi_layout.json", format = "sdist" },
    { path = "cuda/pyproject.toml", format = "sdist" },
    { path = "cuda/src/**/*", format = "sdist" },
]
